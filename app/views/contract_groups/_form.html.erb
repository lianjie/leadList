<%= form_for(@contract_group,:url=>{:action => 'create'},:html=>{:multipart => true}) do |f| %>
  <% if @contract_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contract_group.errors.count, "error") %> prohibited this contract_group from being saved:</h2>

      <ul>
      <% @contract_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :group_name %><br />
    <%= f.text_field :group_name %>
  </div>
  <div class="field">
    <%= f.label :file %><br />
    <%= file_field("file","file") %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
